# ================================
# Frontend: .env.example (Vite/Netlify)
# ================================
# Copy to ".env" for local dev, or set in Netlify as "VITE_*" env vars.
# IMPORTANT: Include the /api prefix so the app hits the FastAPI routes.
VITE_API_BASE_URL="https://sublime-consideration-production.up.railway.app/api"

# Optional: PDF direct link (if frontend talks to PDF service directly)
VITE_PDF_SERVICE_URL=""
